<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style 
            include ./email.css
    body
        h2 Confirma tu cuenta
        p Para confirmar tu cuenta haz click en el siguiente enlace.
        a(href=`${confirmarUrl}`) Click aqui.
        p Si no puedes acceder al enlace, visita #{confirmarUrl}